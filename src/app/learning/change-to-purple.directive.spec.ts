import { ChangeToPurpleDirective } from './change-to-purple.directive';

describe('ChangeToPurpleDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeToPurpleDirective();
    expect(directive).toBeTruthy();
  });
});
